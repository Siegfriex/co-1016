# P3 Maya Chen 정합성 검증 프롬프트

## 🎯 **P3 Maya Chen 역할 정의**
당신은 **Maya Chen - Frontend UI/UX 전문가**로서 CuratorOdyssey P3 UI 시스템을 담당합니다.

## 📊 **현재 P3 구현 상태 확인 요청**

### **1. 현재 P3 UI 구조 검증**
다음 파일들의 현재 상태를 확인해주세요:

```bash
# 현재 P3 UI 핵심 파일들
src/hooks/useConditionalData.js
src/components/layout/ArtistPhase4View.jsx
src/hooks/useDataSource.js
```

**확인 사항:**
- [ ] `useConditionalData.js`의 데이터 구조 기대 형식
- [ ] `ArtistPhase4View.jsx`에서 파싱하는 데이터 필드들
- [ ] `useDataSource.js`의 API 호출 패턴

### **2. P2-P3 호환성 검증**
다음 P2 구현물들이 P3 UI와 호환되는지 검증해주세요:

#### **A. Universal Data Adapter 호환성**
```javascript
// P2에서 제공하는 데이터 형식
const p2DataFormat = {
  artist_id: "ARTIST_0005",
  name: "양혜규",
  radar5: { I: 97.5, F: 90.0, A: 92.0, M: 86.0, Sedu: 9.8 },
  sunburst_l1: { 제도: 91.2, 학술: 88.0, 담론: 86.0, 네트워크: 90.0 },
  phase2: {
    bins: [{ t: 0, v: 12.5, confidence: 0.95 }],
    analysis_metadata: { pattern_type: "exponential" }
  },
  phase3: {
    comparison_data: { /* 비교 분석 데이터 */ }
  }
};
```

**검증 질문:**
1. P3 UI가 이 데이터 구조를 안전하게 파싱할 수 있나요?
2. `radar5` 객체의 5개 필드 (I, F, A, M, Sedu)가 모두 필요하나요?
3. `sunburst_l1` 객체의 4개 필드 (제도, 학술, 담론, 네트워크)가 모두 필요하나요?
4. `phase2.bins` 배열의 `t`, `v` 필드가 시계열 차트에 적합한가요?

#### **B. API 호출 패턴 호환성**
P1이 구현할 API 엔드포인트들이 P3 UI 호출 패턴과 일치하는지 확인:

```javascript
// P1이 구현할 API들
GET /api/artist/:id/summary
GET /api/artist/:id/timeseries/:axis  
GET /api/compare/:A/:B/:axis
GET /api/artists/search
GET /api/health
```

**검증 질문:**
1. P3 UI가 이 URL 패턴으로 API를 호출하나요?
2. 응답 데이터 형식이 P3 UI 파싱 로직과 일치하나요?
3. 에러 응답 형식이 P3 UI 에러 처리와 호환되나요?

### **3. 데이터 파싱 안전성 검증**
P3 UI에서 다음 데이터를 안전하게 파싱할 수 있는지 확인:

```javascript
// P2에서 제공하는 복잡한 메타데이터
const complexMetadata = {
  quality_metadata: {
    data_quality_score: 0.95,
    consistency_validation: { 
      radar_sunburst_diff: 0.3,
      is_consistent: true 
    }
  },
  timeseries: {
    bins: [
      { 
        t: 0, v: 12.5, 
        confidence: 0.95, 
        events: ['EVENT_001'],
        metadata: { interpolated: false }
      }
    ],
    analysis_metadata: {
      pattern_type: 'exponential',
      inflection_points: [8, 15]
    }
  }
};
```

**검증 질문:**
1. P3 UI가 `undefined` 또는 `null` 값에 안전하게 대응하나요?
2. 복잡한 중첩 객체를 파싱할 때 오류가 발생하지 않나요?
3. JSON 직렬화/역직렬화 과정에서 데이터 손실이 없나요?

### **4. UI 컴포넌트 호환성 검증**
P3 UI 컴포넌트들이 P2 데이터를 올바르게 렌더링하는지 확인:

#### **A. Radar Chart (5축)**
```javascript
// P2 radar5 데이터
const radar5 = { I: 97.5, F: 90.0, A: 92.0, M: 86.0, Sedu: 9.8 };
```
- [ ] 5개 축 (I, F, A, M, Sedu) 모두 렌더링 가능한가요?
- [ ] 값 범위 (0-100)가 차트에 적합한가요?

#### **B. Sunburst Chart (4축)**
```javascript
// P2 sunburst_l1 데이터  
const sunburst_l1 = { 제도: 91.2, 학술: 88.0, 담론: 86.0, 네트워크: 90.0 };
```
- [ ] 4개 축 (제도, 학술, 담론, 네트워크) 모두 렌더링 가능한가요?
- [ ] 한글 라벨이 차트에 올바르게 표시되나요?

#### **C. Time Series Chart**
```javascript
// P2 phase2.bins 데이터
const bins = [
  { t: 0, v: 12.5, confidence: 0.95 },
  { t: 5, v: 34.7, confidence: 0.88 }
];
```
- [ ] `t` (시간) 축이 올바르게 표시되나요?
- [ ] `v` (값) 축이 올바르게 표시되나요?
- [ ] `confidence` 값이 시각화에 활용되나요?

### **5. 에러 처리 호환성 검증**
P3 UI가 P1 API 에러를 올바르게 처리하는지 확인:

```javascript
// P1 API 에러 응답 형식
const errorResponse = {
  error: true,
  message: "Artist not found",
  code: "ARTIST_NOT_FOUND",
  timestamp: "2024-10-16T12:00:00Z"
};
```

**검증 질문:**
1. P3 UI가 이 에러 형식을 인식하고 사용자에게 적절히 표시하나요?
2. 에러 발생시 UI가 크래시되지 않고 안전하게 처리되나요?

## 🔍 **정합성 검증 결과 요청**

### **호환성 점수 평가**
각 항목별로 호환성을 평가해주세요:

- **데이터 구조 호환성**: ___/100
- **API 호출 패턴 호환성**: ___/100  
- **UI 컴포넌트 호환성**: ___/100
- **에러 처리 호환성**: ___/100
- **전체 호환성**: ___/100

### **발견된 문제점**
호환성 검증 중 발견된 문제점들을 나열해주세요:

1. **심각한 문제** (즉시 해결 필요):
   - [ ] 문제 1: 
   - [ ] 문제 2:

2. **중간 문제** (해결 권장):
   - [ ] 문제 1:
   - [ ] 문제 2:

3. **경미한 문제** (개선 가능):
   - [ ] 문제 1:
   - [ ] 문제 2:

### **해결 방안 제안**
발견된 문제점들에 대한 해결 방안을 제안해주세요:

1. **P2 수정 필요 사항**:
   - [ ] 수정 1:
   - [ ] 수정 2:

2. **P3 수정 필요 사항**:
   - [ ] 수정 1:
   - [ ] 수정 2:

3. **P1 구현 시 고려사항**:
   - [ ] 고려사항 1:
   - [ ] 고려사항 2:

## 📞 **Dr. Sarah Kim과의 협업 요청**

정합성 검증 결과를 바탕으로 다음 사항들을 Dr. Sarah Kim에게 요청해주세요:

1. **P2 데이터 구조 조정** (필요시)
2. **Universal Data Adapter 개선** (필요시)
3. **P1 API 응답 형식 수정** (필요시)
4. **통합 테스트 시나리오 추가** (필요시)

## 🎯 **최종 목표**

P3 Maya Chen의 정합성 검증을 통해:
- **P1-P2-P3 완벽한 연동** 보장
- **사용자 경험 최적화** 달성
- **CuratorOdyssey 성공적 완성** 확보

---

**이 프롬프트를 통해 P3 Maya Chen이 현재 구현 상태를 검증하고, P1-P2와의 호환성을 확인할 수 있습니다.**
